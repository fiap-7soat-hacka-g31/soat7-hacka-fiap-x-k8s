name: Terraform IAC - Kubernetes Cluster

on:
  pull_request:
    branches:
      - main
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  tf-summary:
    if: github.event_name == 'pull_request'
    name: Terraform Summary
    runs-on: ubuntu-latest
    steps:
      - name: Hello World
        run: echo "Something me doing?"

  tf-plan:
    name: Terraform Plan
    runs-on: ubuntu-latest
    environment:
      name: build
    steps:
      - name: Hello World
        run: echo "Something me doing?"

  tf-apply:
    name: Terraform Apply
    runs-on: ubuntu-latest
    needs: [tf-plan]
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    environment:
      name: production
    steps:
      - name: Hello World
        run: echo "Something me doing?"
